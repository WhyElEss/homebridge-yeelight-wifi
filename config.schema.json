{
  "pluginAlias": "yeelight",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Lamps are discovered over SSDP, so nothing here is required — every field has a working default. Add a lamp under **Devices** only to name it or to make it behave differently from the rest; the id it answers to is printed in the log as `Received advertisement from 78cb4e`.",
  "footerDisplay": "See the [README](https://github.com/WhyElEss/homebridge-yeelight-wifi#readme) for what each setting does and why the defaults are what they are.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Yeelight",
        "required": true
      },
      "alert": {
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Add an alert switch to every lamp",
            "type": "boolean",
            "default": false,
            "description": "Gives every colour-capable lamp a second accessory — a plain switch named after it, like “Entrance Night Light Alert”. Switching it on takes that lamp to the colour below and remembers what it was doing; switching it off puts it back exactly, Adaptive Lighting included. A lamp listed under Devices can override any of this for itself."
          },
          "hue": {
            "title": "Default Alert Hue (0-360)",
            "type": "integer",
            "default": 0,
            "description": "0 red · 30 orange · 50 yellow · 120 green · 195 cyan · 240 blue · 280 purple · 320 pink"
          },
          "saturation": {
            "title": "Default Alert Saturation (0-100)",
            "type": "integer",
            "default": 100,
            "description": "100 is the pure colour; lower values wash it out towards white."
          },
          "brightness": {
            "title": "Default Alert Brightness (1-100)",
            "type": "integer",
            "description": "Leave empty to keep whatever brightness the lamp already had, which is also one command less on the wire."
          }
        }
      },
      "transitions": {
        "type": "object",
        "properties": {
          "power": {
            "title": "Power (ms)",
            "type": "integer",
            "default": 400,
            "minimum": 0
          },
          "brightness": {
            "title": "Brightness (ms)",
            "type": "integer",
            "default": 400,
            "minimum": 0
          },
          "color": {
            "title": "Colour (ms)",
            "type": "integer",
            "default": 400,
            "minimum": 0
          },
          "temperature": {
            "title": "Colour Temperature (ms)",
            "type": "integer",
            "default": 400,
            "minimum": 0
          }
        }
      },
      "connection": {
        "type": "object",
        "properties": {
          "timeout": {
            "title": "Reply Timeout (ms)",
            "type": "integer",
            "default": 2000,
            "minimum": 200,
            "description": "Lamps on Wi-Fi routinely need 100-200 ms, so anything under ~1000 mostly measures the network."
          },
          "retries": {
            "title": "Retries",
            "type": "integer",
            "default": 1,
            "minimum": 0,
            "maximum": 5,
            "description": "Extra attempts after a failure, each on a fresh connection. A command is never written twice to the same socket."
          },
          "connectTimeout": {
            "title": "Connect Timeout (ms)",
            "type": "integer",
            "default": 5000,
            "minimum": 500
          },
          "quota": {
            "title": "Commands per minute",
            "type": "integer",
            "default": 55,
            "minimum": 1,
            "maximum": 60,
            "description": "The firmware allows 60 and hangs up when that is exceeded, so the default leaves a margin. Raising this is how lamps used to go deaf mid-automation."
          },
          "coalesce": {
            "title": "Coalescing Window (ms)",
            "type": "integer",
            "default": 80,
            "minimum": 0,
            "description": "Characteristic writes landing inside this window are merged into one command. Raising it merges more; lowering it makes the lamp react sooner."
          },
          "keepAlive": {
            "title": "TCP Keep-Alive (ms)",
            "type": "integer",
            "default": 30000,
            "minimum": 1000
          }
        }
      },
      "multicast": {
        "type": "object",
        "properties": {
          "interface": {
            "title": "Multicast Interface",
            "type": "string",
            "placeholder": "0.0.0.0",
            "description": "Set a specific address when the host has several and discovery binds to the wrong one."
          }
        }
      },
      "defaultValue": {
        "type": "object",
        "additionalProperties": true
      },
      "devices": {
        "title": "Devices",
        "type": "array",
        "description": "Per-lamp settings. Lamps not listed here still work with the platform defaults.",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "title": "Device ID",
              "type": "string",
              "required": true,
              "description": "The full id (0x000000001778cb4e), its last six characters (78cb4e), or the model-and-id name (bslamp3-78cb4e) — whichever you find readable. It is printed in the log."
            },
            "name": {
              "title": "Name",
              "type": "string",
              "description": "The name shown in HomeKit. Defaults to the model and id, like bslamp3-78cb4e."
            },
            "hidden": {
              "title": "Hide this lamp",
              "type": "boolean",
              "description": "Keeps the whole lamp out of HomeKit, without deleting its entry here."
            },
            "blacklist": {
              "title": "Hide from HomeKit",
              "type": "array",
              "description": "Advanced, and edited in the JSON editor: the lamp capabilities to keep out of HomeKit.",
              "items": {
                "type": "string",
                "oneOf": [
                  {
                    "title": "Brightness",
                    "const": "set_bright"
                  },
                  {
                    "title": "Colour",
                    "const": "set_hsv"
                  },
                  {
                    "title": "Colour temperature — also removes Adaptive Lighting",
                    "const": "set_ct_abx"
                  },
                  {
                    "title": "Moonlight mode (ceiling lamps)",
                    "const": "active_mode"
                  },
                  {
                    "title": "Backlight",
                    "const": "bg_set_power"
                  },
                  {
                    "title": "Backlight brightness",
                    "const": "bg_set_bright"
                  },
                  {
                    "title": "Backlight colour",
                    "const": "bg_set_hsv"
                  }
                ]
              }
            },
            "alert": {
              "type": "object",
              "properties": {
                "enabled": {
                  "title": "Alert switch for this lamp",
                  "type": "boolean",
                  "description": "Overrides the platform setting: tick to give this lamp an alert switch even when the default is off, untick to leave this one lamp without one."
                },
                "hue": {
                  "title": "Alert Hue for this lamp (0-360)",
                  "type": "integer",
                  "description": "Leave empty to use the default colour set above. 0 red · 30 orange · 50 yellow · 120 green · 195 cyan · 240 blue · 280 purple · 320 pink"
                },
                "saturation": {
                  "title": "Alert Saturation for this lamp (0-100)",
                  "type": "integer",
                  "description": "Leave empty to use the default."
                },
                "brightness": {
                  "title": "Alert Brightness for this lamp (1-100)",
                  "type": "integer",
                  "description": "Leave empty to use the default, which is to keep whatever brightness the lamp already had."
                }
              }
            }
          }
        }
      }
    }
  },
  "layout": [
    "name",
    {
      "type": "fieldset",
      "title": "Alert switch — defaults for every lamp",
      "expandable": true,
      "expanded": true,
      "items": [
        "alert.enabled",
        {
          "key": "alert.hue",
          "condition": {
            "functionBody": "return model.alert && model.alert.enabled"
          }
        },
        {
          "key": "alert.saturation",
          "condition": {
            "functionBody": "return model.alert && model.alert.enabled"
          }
        },
        {
          "key": "alert.brightness",
          "condition": {
            "functionBody": "return model.alert && model.alert.enabled"
          }
        }
      ],
      "description": "What an alert does to a lamp, unless that lamp says otherwise under Devices."
    },
    {
      "key": "devices",
      "type": "array",
      "title": "Devices",
      "expandable": true,
      "expanded": true,
      "items": [
        "devices[].id",
        "devices[].name",
        "devices[].hidden",
        "devices[].alert.enabled",
        "devices[].alert.hue",
        "devices[].alert.saturation",
        "devices[].alert.brightness"
      ],
      "description": "Settings for one lamp. Anything left empty here is inherited from the defaults above."
    },
    {
      "type": "fieldset",
      "title": "Connection",
      "description": "These pace the plugin inside the lamp's LAN command budget. The defaults were measured against real hardware.",
      "expandable": true,
      "expanded": false,
      "items": [
        "connection.timeout",
        "connection.retries",
        "connection.connectTimeout",
        "connection.quota",
        "connection.coalesce",
        "connection.keepAlive"
      ]
    },
    {
      "type": "fieldset",
      "title": "Transitions",
      "description": "How long the lamp takes to fade to a new value. Waking a lamp uses set_scene, which the firmware applies without a transition.",
      "expandable": true,
      "expanded": false,
      "items": [
        "transitions.power",
        "transitions.brightness",
        "transitions.color",
        "transitions.temperature"
      ]
    },
    {
      "type": "fieldset",
      "title": "Discovery",
      "expandable": true,
      "expanded": false,
      "items": ["multicast.interface"]
    }
  ]
}
